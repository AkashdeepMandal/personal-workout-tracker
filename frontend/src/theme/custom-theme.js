import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#009688",
    },
    shades: {
      light: "#fff",
      main: "#d1d5db",
      dark: "#757575",
      darker: "#212121",
    },
  },
});
